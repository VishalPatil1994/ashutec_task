import { ShowDeletedPipe } from './show-deleted.pipe';

describe('ShowDeletedPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowDeletedPipe();
    expect(pipe).toBeTruthy();
  });
});
